[build]
builder = "RAILPACK"
buildCommand = "python -m playwright install --with-deps chromium"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
