[build]
builder = "RAILPACK"
buildCommand = "PLAYWRIGHT_BROWSERS_PATH=0 python -m playwright install --with-deps chromium"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
